# ACTIVE.md — Dávidova cesta
_The ONE task this session is doing, plus the ordered next-pick queue. Read on
bootstrap. NOT a history log — shipped cards leave here entirely._

## NOW
**Fáza 4 — animácie** (po potvrdení): obr.6 ODOMKNUTIE (rozšíriť terajšiu jednoduchú výmenu pozadia)
+ obr.7 ODHALENIE SYMBOLU + obr.8 NÁVRAT NA MAPU + obr.9 POKRAČOVANIE ZAJTRA.
**Stavia na Fáze 3:** konzumuje pergamenový tok (`skusOdomknut` → `ukazOdomknutie`) a `DNI` config.
Kontrakt clue (`cx/cy` = % písacej plochy) + odomknutie + kľúč `davidovaCesta.v1` sú v
`memory/project_build_plan.md`. **Pozor (BRS obr.9):** „Pokračovanie zajtra" sa NEzobrazí pri D5
(D5 má vlastnú sekvenciu = Fáza 5). Save PRED animáciou zostáva (EC-003).

## NEXT (ordered next-pick queue)
1. Fáza 4 — animácie (odomknutie rozšíriť + symbol + návrat + zajtra).
2. Fáza 5 — D5 špeciál (finálna mapa, TOTEM, SIFRA, 13177, truhlica).
3. Fáza 6 — zvuk (mp3 + tichý fallback).

## Recently shipped
- Fáza 3 — pergameny (clue + heslo + nesprávne) + obsah 5 dní; odomknutie cez ODOMKNUTIE.png — S4.
- Fáza 1 (kostra INTRO+MAPA) + Fáza 2 (stav + localStorage + odomknutie + reset) — S3.
